% Intentional errors. Based off interim1_ex2.txt %

DEFINE 
    CLK1 AS CLOCK; % no args for CLOCK should give error % 
    SW1 AS SWITCH WITH ; % missing args for initial condition should give error %   

    G1 AS AND WITH inputs=2; 
    G2 AS ; % no gate type given should give error % 
    DTYPE1, DTYPE2 AS DTYPE; % no commas in name should give error %
    DTYP 3 AS DTYPE; % typo for 'DTYPE' should give error %

CONNECT CLK1 = DTYPE1, % not specifying which port of DTYPE1 should give error %
    CLK1 = DTYPE3.CLK,
    CLK1 = G1.I1,
    DTYPE1.QBAR = DTYPE1.DATA,
    DTYPE1.Q = DTYPE2.CLK,
    DTYPE2.QBAR = DTYPE2.DATA,
    DTYPE3.DATA = DTYPE2.Q, % input/output order flipped %
    DTYPE3.Q = G1.I2,
    G2.I1, % missing equals sign/arg should give error %
    SW11 = G2.I2; % there is no 'SW11' defined, should give error %

MONITOR DTYPE1.Q, 
    DTYPE2.Q, 
    G2;

END;