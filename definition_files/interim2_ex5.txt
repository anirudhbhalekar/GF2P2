% Original example in handout, but with no monitor points initially so we can try adding %

DEFINE
    G1 AS NAND WITH inputs=2, 
    G2 AS NAND WITH inputs=2,
    SW1 AS SWITCH WITH initial=0,
    SW2 AS SWITCH WITH initial=0;	

CONNECT 
    G1.I1 = SW1,
    G1.I2 = SW2;    

MONITOR ;

END;